name: M6 Task 15 retirement verifier

on:
  push:
    branches:
      - feat/technology-modernization-m1

jobs:
  retirement-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Rebuild locked workspace
        run: |
          python -m pip install uv
          uv sync --locked --all-packages
      - name: Run M6 retirement contract
        run: uv run python -m pytest tests/architecture/test_modernization_retirement.py -q
      - name: Run absolute Ruff
        run: uv run ruff check --select E,F,I tests/architecture/test_modernization_retirement.py
