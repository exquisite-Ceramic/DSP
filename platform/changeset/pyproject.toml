[project]
name = "changeset"
version = "0.1.0"
description = "Change-set modeling and execution slicing for the design agent. Phase 2."
requires-python = ">=3.11"
dependencies = ["host-contracts>=0.1.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
