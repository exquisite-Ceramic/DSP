[project]
name = "design-materialization-planning"
version = "0.1.0"
description = "Provider-neutral deterministic materialization planning contracts for DSP."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
