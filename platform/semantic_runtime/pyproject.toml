[project]
name = "semantic-runtime"
version = "0.1.0"
description = "Semantic runtime for the design agent: entity identity, dirty map, snapshots, journal. Phase 2."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
