# DSP Modernization Ledger

**Record state:** Technology Modernization closeout complete through Task 16; M0–M4 evidence retained; M5 `NO_CUTOVER`; M6 `NO_RETIREMENTS`; T4 findings handed to Architecture Modernization Review
**Post-Phase-I clean baseline:** `e308e9279d17ab61ef0d30c874942ce273a0a3f9`  
**Modernization execution base:** `2edb734c9aa26a32b414a0eff891260831009a97`  
**Observed / frozen:** 2026-09-16  
**Design authority:** `docs/superpowers/specs/2026-09-13-dsp-modernization-design.md`  
**Implementation plan:** `docs/superpowers/plans/2026-09-13-dsp-modernization.md`

Task 2 completed the factual A1-A7 inventory and Task 3 froze the execution boundary. `Execution state` preserves that M0 authorization history. At Task 16, `Status` is terminal: `VERIFIED` means the approved disposition satisfied its Technology Modernization gate; `CLOSED_NO_CUTOVER` means compatibility evidence was retained but canonical ownership did not switch; `DEFERRED` / `REJECTED` remain non-executable outcomes. No row remains in `Status = APPROVED`.

Operational disposition values are `KEEP`, `PIN_LOCK`, `UPGRADE`, `MIGRATE`, and `DEFER_ARCHITECTURE`. If implementation evidence later shows that an approved item requires public-contract, canonical-semantic, Host-visible, orchestration, identity/materialization/Saga, or capability changes, it is immediately escalated to T4 and moved to `DEFER_ARCHITECTURE / DEFERRED`.

| ID | Area | Current | Candidate | Risk | Decision | Owner | Consumers | Status | Execution state | Evidence | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MOD-001 | Python runtime | root `>=3.11`; canonical CI 3.11; Ruff `py311` | add Python 3.14 compatibility lane, then cut over only at M5 after parity | T2 | UPGRADE | Platform Runtime | all first-party Python packages, tests, CI | CLOSED_NO_CUTOVER | APPROVED | Python 3.14.7 official release observed 2026-09-15; Task 7 canonical run `34987027986` at `b4af4264a35f1d2d799b44d62d5735457a132cd2` proved Python 3.11 canonical and Python 3.14.7 compatibility lanes both reconstruct the same committed workspace/lock and both pass importlib plus local-parity pytest with `1475 passed / 17 skipped / 1 warning`; Revit Core stayed GREEN; Ruff remained canonical-3.11-only with `320 -> 320 / new 0`; absolute Ruff verifier `34987662684` at `68a5ee77acd672d766964df5114ddc2a1be736b0` proved the Task 7 architecture tests E/F/I-clean. Root `requires-python >=3.11` and Ruff `py311` remain unchanged, so this is compatibility evidence only, not the M5 cutover Task 14 cutover decision `NO_CUTOVER`: M5 re-read the frozen ledger before changing canonical ownership and found MOD-001 still `APPROVED / APPROVED`, not `VERIFIED`. Task 7 therefore remains compatibility evidence only. The coordinated M5 gate intentionally leaves root `requires-python >=3.11`, Ruff `py311`, Python 3.11 canonical CI, and Python 3.14 compatibility unchanged. The new `test_modernization_canonical_consistency.py` prevents a future silent promotion while this ledger state remains unverified. Task 16 terminal disposition: `CLOSED_NO_CUTOVER`; the verified Python 3.14 compatibility lane is retained, but M5 did not transfer canonical ownership from Python 3.11 and M6 therefore retired nothing. | retain 3.11 canonical lane/floor until M5 cutover proof; remove/revert the 3.14 compatibility matrix row if later parity regresses |
| MOD-002 | Python workspace | multiple package manifests plus procedural editable installs/root pythonpath | root `uv` workspace with explicit first-party graph while preserving package identities | T1 | MIGRATE | Build & Release | package-managed Python distributions and source-only import paths | VERIFIED | APPROVED | Task 4 locked verification run `34981121854` at `301fa89fe4028920c3911332e50630088c5f7e36` proved the M0-derived 19-member workspace; Task 5 canonical cutover run `34984076721` at `d03815776e9a1c49db70d95e2d266dedbffe1263` rebuilt the workspace from committed metadata/lock, resolved 71 packages, and passed both uv pytest modes with `1466 passed / 17 skipped`, Ruff `319 -> 319 / new 0`, and Revit Core GREEN | revert canonical workflow to the last known-green pip/editable bootstrap and revert workspace metadata/lock only if locked reconstruction or parity regresses |
| MOD-003 | Python resolution | no repository-wide lock | one committed root `uv.lock`, consumed frozen by dev/CI after parity | T1 | PIN_LOCK | Build & Release | all Python dev/CI environments | VERIFIED | APPROVED | resolver-generated committed `uv.lock`; Task 4 run `34981121854` proved `uv lock --check` and clean locked reconstruction; Task 5 run `34984076721` at `d03815776e9a1c49db70d95e2d266dedbffe1263` proves canonical CI now executes `uv sync --locked --all-packages`, both canonical pytest modes and Ruff from that committed graph with no new diagnostics | revert `.github/workflows/repository-regression.yml` to the previous pip/editable bootstrap and retain the last known-green lock for investigation if locked CI regresses |
| MOD-004 | Python build backend | setuptools observed for packaged components | retain setuptools during Technology Modernization | T0 | KEEP | Build & Release | packaged Python distributions | VERIFIED | APPROVED | inspected manifests retain setuptools; no concrete backend blocker found by M0 Task 16 terminal disposition: `VERIFIED`; all locked workspace rebuilds through M6 continued to build the packaged components with the retained setuptools backend, and no backend-specific blocker or behavior change was required. | no change; preserve setuptools metadata/backend |
| MOD-005 | JSON Schema resolver | `jsonschema>=4.20`; canonical regression emitted `RefResolver` deprecation warning | characterize equivalence, then migrate implementation to `referencing.Registry` without schema-semantic change | T1 | MIGRATE | Schema Tooling | JSON Schema validators/contract tests | VERIFIED | APPROVED | Task 9 old-resolver characterization run `34990765623` proved the frozen schema-validation boundary `9/9` GREEN before migration. RED run `34990942372` then kept all 9 characterization tests GREEN while the implementation guard failed only because `contracts/python/tests/test_normalized_design_fact_schema.py` still contained `RefResolver`. Migration commit `81f47e99fd5b93f993b665f178f12c3b90251030` replaced that mechanism with `referencing.Registry`/`Resource` using the existing canonical schema `$id`; focused run `34991121800` passed `10/10` with the deprecated resolver absent. Canonical run `34991121752` passed Python 3.11 and 3.14 in both pytest modes with `1482 passed / 17 skipped`, Ruff `319 -> 319 / new 0`, and both Revit net8/net10 jobs GREEN. Absolute Ruff verifier `34992076721` at `43288cd89319ae0a72e596f91cb4e44af00879fd` proved both Task 9 files E/F/I-clean after the formatting-only fix. Commit comparison from Task 8 final `0f8104639f18f542fdff71442ba611e366db94d5` to the Task 9 candidate changed only the schema conformance test, architecture guard, and temporary verifier; `contracts/schemas/` was untouched | revert the `Registry` validation helper to the characterized resolver mechanism while retaining characterization tests and the unchanged canonical schemas if reference-resolution parity regresses |
| MOD-006 | MCP SDK | `mcp>=2,<3` in semantic MCP and AutoCAD sidecar | remain on supported MCP Python SDK v2 line and lock exact resolution | T1 | KEEP | Semantic Transport | semantic MCP adapter, AutoCAD MCP entrypoint | VERIFIED | APPROVED | Official `modelcontextprotocol/python-sdk` latest non-prerelease release observed 2026-09-16 is `v2.2.0` (published 2026-09-07). Task 10 restored-graph verifier `35031391276` at `938111bb1938dfc7d6a299931d44d4a0602fe43d` reconstructed the committed lock with `mcp=2.2.0` and passed the MCP-focused suite with `76 passed / 1356 deselected`. Because the existing range and committed lock already resolve the current supported v2 release, Task 10 requires no MCP manifest or `uv.lock` mutation | retain the current v2 API/range and committed lock; if upstream support status or a future approved refresh changes, handle it as a separately verified dependency-family change |
| MOD-007 | gRPC/Protobuf | Python grpc/protobuf ranges; AutoCAD .NET Google.Protobuf 3.29.3 and Grpc 2.70.0; generated Python stubs committed | first lock the proven current graph; any later version refresh requires generator/runtime parity and unchanged proto semantics | T1/T2 | PIN_LOCK | Host Transport | AutoCAD sidecar/plugin transport, Host contracts | VERIFIED | APPROVED | Task 6 made current AutoCAD transport ownership machine-verifiable. Task 10 baseline run `34993564545` froze the actual graph: Python `grpcio=1.84.0`, `protobuf=5.29.6`, runtime sync without `grpcio-tools`; .NET `Google.Protobuf=3.29.3` and gRPC `2.70.0`; committed Python stubs report Protobuf generator `5.29.0` and gRPC generator `1.70.0`; focused transport passed `71` Python tests and `14/14` .NET tests. A controlled .NET refresh experiment then exposed the complete consumer boundary: run `35030869796` failed restore with `NU1605` because `Grpc.AspNetCore 2.83.0` required `Grpc.Net.Client >=2.83.0` while the test project still pinned `2.70.0`; after aligning that test consumer, focused run `35031059959` and canonical run `35031059916` were GREEN. The frozen MOD-007 candidate nevertheless requires generator/runtime parity before any version refresh, which is Task 12 scope, so every experimental version delta was rolled back. Comparison from Task 9 final `c22ce401fdda59ccde1ad82d89069d78e6b5da53` to restored head `938111bb1938dfc7d6a299931d44d4a0602fe43d` leaves only the temporary verifier. Restored run `35031391276` passed the ownership contract `7/7`, Python transport `71 passed / 1361 deselected`, MCP `76 passed / 1356 deselected`, AutoCAD .NET gRPC `14/14`, and unchanged generated provenance. This verifies the `PIN_LOCK` disposition; the actual runtime/tooling refresh remains gated on Task 12 reproducibility and wire-parity evidence Task 12 then made the frozen graph reproducible without a version refresh. `hosts/autocad/sidecar/pyproject.toml` now declares `[tool.dsp.proto-codegen]` for the canonical source, existing generated-package output, and regeneration command, while `grpcio-tools==1.70.0` remains exact and is resolved from the committed `uv.lock`. Task 12 codegen verifier `35034932261` at `b7c01dfd319ff0c5afc3450570c916e84d377ee6` installed that locked generator (`libprotoc 29.0`), regenerated only into a temporary tree, and proved exact `FileDescriptorProto` parity including package, proto3 syntax, C# namespace, message field numbers/types, service methods, and serialized descriptor SHA-256 `0940b1ded0cd508794556cd7cbcc229c20e042275094702dbba5c7aed246418c`; the Ping/Dispatch gRPC method paths were unchanged and `git diff` proved the canonical proto and committed generated tree were untouched. The same focused run passed Python transport `72 passed / 1370 deselected`, AutoCAD .NET gRPC `14/14`, the architecture contract `5/5`, and absolute E/F/I Ruff. Same-head canonical run `35034932346` passed Python 3.11 and 3.14 in both pytest modes with `1492 passed / 17 skipped`, Ruff `320 -> 319 / new 0`, and Revit net8/net10 GREEN. This satisfies the Task 12 reproducibility/wire-parity prerequisite for the current `PIN_LOCK` disposition; it does not itself authorize a runtime/tooling version refresh. | retain the frozen current runtime/tooling graph and canonical proto; if Task 12 parity fails, keep these versions and generated outputs unchanged rather than forcing a refresh |
| MOD-008 | GitHub Actions | hosted workflows now use `actions/checkout@v7`, `actions/setup-python@v7`, and `actions/setup-dotnet@v6`; the Phase I real dual-Host self-hosted job remains the sole compatibility exception at `checkout@v4` / `setup-python@v5` pending runner proof | retain the supported Node-24-capable hosted Action majors while preserving the explicit self-hosted exception until its runner is proven `>=2.327.1` | T0 | UPGRADE | Build & Release | canonical + focused GitHub workflows | VERIFIED | APPROVED | Task 11 upgraded each family separately: checkout commit `b6dfe5292f169874678b521eaf173ed1f6ffdc35`, setup-python commit `1c89c83cd3b7a60054c44ceb6a4f4f636eb79c2d`, setup-dotnet commit `f091ebccdda0177f5fe2732ce8d17ea50635e5f8`. Family verifiers proved hosted migration while `tests/architecture/test_modernization_actions.py` keeps the Phase I self-hosted tail as the only deferred exception. Exact-head structural run `35033696892` at `38f31acf312c43f02d5ccfa6d279c4b9a67d8fb9` passed the three Task 11 workflow architecture files `17/17` plus absolute E/F/I Ruff. Canonical run `35033696790` on the same head passed Python 3.11 and 3.14 in both pytest modes with `1487 passed / 17 skipped`, Ruff `319 -> 319 / new 0`, and Revit net8/net10 `54/54`; the hosted runner reports `2.337.0`, satisfying Node-24 Action requirements. No unverified upgrade was applied to the required Windows self-hosted real-Host job | revert hosted Action refs to the previous known-green majors if workflow parity regresses; keep the self-hosted exception unchanged until its runner-version evidence satisfies the Node-24 minimum |
| MOD-009 | Revit Core | default/canonical `net8.0`; compatibility target opt-in only | prove `net10.0` compatibility in Host-neutral lane; no native Host retarget is implied | T2 | UPGRADE | Revit Host / Platform Runtime | Revit Core.Tests and native Revit plugin consumer | VERIFIED | APPROVED | Task 8 first proved that unconditional `net8.0;net10.0` was unsafe for existing 8.x consumers: run `34988924018` at `c30d6e1462437da3397cd6df56b20dc86fd335d7` failed with `NETSDK1045`, and the same leakage broke the Revit Core steps in Phase H `34988924001` and Phase I `34988923987`. The accepted implementation keeps default `TargetFrameworks=net8.0` and exposes `net8.0;net10.0` only under `DspEnableNet10Compatibility=true`. Canonical run `34989313437` at `9edbf7c9b10fb60bb28ddfed80d075622233fb9c` passed Core.Tests `54/54` with SDK 8.0.425 on net8 and `54/54` with SDK 10.0.401 on opt-in net10; Python 3.11/3.14 both passed `1477 / 17` in both pytest modes and Ruff stayed `319 -> 319 / new 0`. Phase H run `34989281811` and Phase I run `34989282011` passed after the opt-in isolation, and absolute Ruff verifier `34989606896` proved the Task 8 architecture test E/F/I-clean. Native Revit/AutoCAD TFMs and root `global.json` were not retargeted | remove the conditional net10 target and `.NET 10 (Host-neutral compatibility)` job while retaining the default net8 target and current Host matrices if compatibility regresses |
| MOD-010 | Revit native plugin | `$(DspRevitTargetFramework)` plus version/API-dir inputs | preserve Host-defined TFM/version matrix; expand only with vendor and real-Host proof | T3 | KEEP | Revit Host | supported Revit native builds | VERIFIED | APPROVED | native csproj; Autodesk Revit 2025 requires .NET 8; T3 real-host evidence required for every new matrix row Task 13 executable Host matrix records only the already-proven Revit 2027 native row as `SUPPORTED`: Autodesk Revit 2027 uses .NET 10 and its add-in guidance requires a .NET 10 SDK; `docs/runbooks/revit-wall-thickness-gap-closure.md` records Revit build `27.2.0.39` at `c8b652f61d9b0aebc1c8d4c5938679b5855c71c3`, `net10.0-windows`, native build PASS with 0 errors, and real Revit acceptance PASS including the 300 mm mutation, stable precommit negatives, replay, and reconciliation. Phase I PR #30 head `502fac1a2ea174f716e30e16b4b8ca5b4e061879` records the real positive dual-Host and partial-commit gates PASS. Task 13 focused verifier `35037478339` at `b742699906c7b5fbc4f6dbb35e2aab1feff37e97` passed the Host-matrix contract `2/2`, absolute E/F/I Ruff, and proved the Revit native plugin boundary is byte-identical to post-Phase-I baseline `e308e9279d17ab61ef0d30c874942ce273a0a3f9` (csproj blob `5730b9c91035fda50f65475596276dcb3107eb67`). Same-head canonical run `35037478336` passed Python 3.11/3.14 in both pytest modes with `1494 passed / 17 skipped`, Ruff `321 -> 319 / new 0`, and Revit net8/net10 GREEN. This verifies the `KEEP` disposition for the explicit Revit 2027 row; Revit 2025 and any other unproven release remain `NOT VERIFIED`, and Task 13 makes no native or Host-visible implementation change. | retain previously accepted Host/TFM combination; do not broaden support claim |
| MOD-011 | AutoCAD native plugin | `net8.0-windows` current target for AutoCAD 2025 baseline | preserve Host-defined runtime matrix; future Host versions are separate T3 rows | T3 | KEEP | AutoCAD Host | supported AutoCAD native builds | VERIFIED | APPROVED | native csproj; Autodesk: AutoCAD 2025/2026 use .NET 8 and 2027 uses .NET 10 Task 13 executable Host matrix records only the already-proven AutoCAD 2025 native row as `SUPPORTED`: Autodesk documents AutoCAD 2025 on .NET 8; the native project remains `net8.0-windows` and resolves `AcCoreMgd.dll`, `AcDbMgd.dll`, and `AcMgd.dll` from `$(AUTOCAD_ACAD_DIR)`. `docs/runbooks/autocad-grpc-smoke.md` records the AutoCAD 2025 SDK build and all 12 real-Host gates PASS, with the latest AutoCAD-specific Gate 11/12 commit `157c54d88fc401ede20944c5d477dbb3e0d95bf6`; Phase I PR #30 head `502fac1a2ea174f716e30e16b4b8ca5b4e061879` records the real positive dual-Host and partial-commit gates PASS. Task 13 focused verifier `35037478339` at `b742699906c7b5fbc4f6dbb35e2aab1feff37e97` passed the Host-matrix contract `2/2`, absolute E/F/I Ruff, and proved the AutoCAD native plugin boundary is byte-identical to post-Phase-I baseline `e308e9279d17ab61ef0d30c874942ce273a0a3f9` (csproj blob `6173973bf5e221c0a34197c50fc978ec5481f58a`). Same-head canonical run `35037478336` passed Python 3.11/3.14 in both pytest modes with `1494 passed / 17 skipped`, Ruff `321 -> 319 / new 0`, and Revit net8/net10 GREEN. This verifies the `KEEP` disposition for the explicit AutoCAD 2025 row; AutoCAD 2026/2027 remain `NOT VERIFIED`, and Task 13 makes no native or Host-visible implementation change. | retain current AutoCAD 2025/net8 baseline; no new Host claim without real acceptance |
| MOD-012 | .NET SDK selection | `global.json` pins 8.0.100 with `latestFeature`; canonical CI uses .NET 8.x | introduce supported .NET 10 SDK compatibility for Host-neutral work, then cut over only when all consumers permit | T1/T2 | UPGRADE | Build & Release | all .NET projects/tests | CLOSED_NO_CUTOVER | APPROVED | Task 6 preserves `global.json` at `8.0.100` + `latestFeature` + `allowPrerelease=false`; verifier run `34985374533` selected SDK `8.0.425` under that policy and passed Revit Core `54/54` plus AutoCAD gRPC transport `14/14`. Task 8 canonical run `34989313437` separately proved SDK 10.0.401 can build/test Host-neutral Revit Core `54/54` through an ephemeral subtree-local selector while the committed root 8.x SDK policy remains unchanged; this is compatibility evidence, not repository-wide .NET 10 cutover proof Task 14 cutover decision `NO_CUTOVER`: M5 found MOD-012 still `APPROVED / APPROVED`, not `VERIFIED`, so Task 8 remains Host-neutral .NET 10 compatibility evidence rather than repository-wide SDK-cutover authorization. Root `global.json` stays on SDK 8.0.100 with `latestFeature`, Revit Core stays canonical `net8.0`, and the opt-in net10 lane remains compatibility-only. Native Host targets remain owned by the Task 13 Host matrix. Task 16 terminal disposition: `CLOSED_NO_CUTOVER`; .NET 10 Host-neutral compatibility remains proven, but M5 did not transfer repository-wide SDK ownership from the .NET 8 policy and M6 therefore retired nothing. | restore prior `global.json`/CI SDK family if a later SDK-family experiment breaks projects or Host consumers; remove the isolated .NET 10 lane without changing the canonical 8.x selector if compatibility regresses |
| MOD-013 | NuGet governance | project-local PackageReference versions | keep project-local ownership unless later duplication/drift evidence proves centralization is needed | T1 | KEEP | Build & Release / .NET owners | contracts, AutoCAD transport, Revit tests | VERIFIED | APPROVED | Task 6 retains project-local ownership and keeps `Directory.Packages.props` absent; AutoCAD transport versions are now single project-level property owners consumed by the existing `PackageReference`s; structural ownership tests plus verifier run `34985374533` prove restore/build/test parity Task 16 terminal disposition: `VERIFIED`; Task 6 ownership checks and subsequent .NET regressions retained project-local NuGet ownership without evidence requiring central package management. | revert the project-level property indirection to the same direct project-local version literals if needed; do not introduce central package management without a separate approved decision |
| MOD-014 | Dependency automation | bounded Dependabot is committed for exactly `github-actions`, `uv`, `nuget`, and `dotnet-sdk`; all schedules are weekly with PR limits 2-3 and no auto-merge policy | retain governed candidate generation only for the approved ecosystems; every generated change still passes normal review and regression gates | T0/T1 | UPGRADE | Build & Release | dependency manifests and CI tooling | VERIFIED | APPROVED | Separate automation commit `34e35fa67e3047e56dbf6f85148548f69981b4bc` added `.github/dependabot.yml` without combining an Action-major change. `tests/architecture/test_modernization_actions.py` enforces the exact four approved ecosystems, weekly cadence, bounded `open-pull-requests-limit`, and absence of auto-merge. Exact-head structural run `35033696892` passed all Task 11 workflow governance tests and canonical run `35033696790` stayed GREEN on both Python lanes and both Revit compatibility lanes | remove `.github/dependabot.yml` or narrow the affected ecosystem if generated update behavior proves unsafe; do not add ecosystems, auto-merge, or Host/runtime cutovers without a separate approved decision |
| MOD-015 | Python typing gate | no repository-wide canonical static typing gate | retain current tests/Ruff; reconsider advisory Pyright/mypy outside the required modernization baseline | T0/T1 | KEEP | Platform Tooling | Python packages/developers | DEFERRED | DEFERRED | no current canonical gate, no frozen tool choice, and no evidence typing is required for runtime/dependency support goals | no change; existing tests and Ruff remain canonical quality gates |
| MOD-016 | V1/V2 compatibility bridges | active V1/V2 paths across planning/binding/reconciliation | Architecture Modernization Review only | T4 | DEFER_ARCHITECTURE | Architecture Review | domain/runtime callers relying on compatibility behavior | DEFERRED | DEFERRED | frozen tree contains active compatibility paths; removal can affect public semantics/contracts/orchestration | preserve bridges; no Technology Modernization removal or redesign |


## Task 16 Technology Modernization closeout

**Program record:** `COMPLETED` on the modernization implementation stack. This status closes the Technology Modernization plan itself; it does not imply that the stacked modernization commits have already been merged to `main`, and it does not define the next capability phase.

- M0–M4 produced reproducible tooling, compatibility evidence, dependency/API modernization, protocol/codegen reproducibility, and explicit Host support evidence without changing canonical product semantics.
- M5 closed MOD-001 and MOD-012 as `CLOSED_NO_CUTOVER`; Python 3.11 and the root .NET 8 SDK policy retain canonical ownership while Python 3.14 / Host-neutral .NET 10 remain compatibility evidence.
- M6 recorded `NO_RETIREMENTS`; no old runtime, SDK, native Host boundary, or architecture compatibility bridge was deleted without the required cutover and merged-main observation evidence.
- MOD-015 remains `DEFERRED`; a repository-wide static typing gate was not required for this program.
- MOD-016 remains T4 / `DEFER_ARCHITECTURE` / `DEFERRED` and is handed to `architecture-modernization-review-input.md` as evidence only.
- Next capability phase remains `NOT YET DEFINED`.

## M0 exit gate

The M0 gate is satisfied only when the exact PR head proves all of the following:

- A1 Runtime, A2 Dependency, A3 Deprecation, A4 Build & Packaging, A5 Protocol & Schema, A6 Host Compatibility, and A7 CI & Toolchain are represented in the M0 records.
- Every MOD-001 through MOD-016 row has owner, consumers, risk, operational decision, execution state, evidence, and rollback ownership.
- No item remains `PROPOSED`; each is `APPROVED`, `DEFERRED`, or `REJECTED`.
- MOD-016 and any future T4 finding are not executable by Technology Modernization.
- M0 changes remain limited to governance documentation/tests and do not change runtime, dependency, Host, protocol/schema semantics, product behavior, or public contracts.
- The focused governance test and canonical repository regression are GREEN on the same exact PR head.

Until those checks are green, M1 is blocked. After they are green, M1 remains **NOT STARTED** until the M0 gate is reviewed/accepted; the M0 commit itself does not perform M1 work.

## M6 Task 15 retirement decision

**Decision:** `NO_RETIREMENTS`

Task 15 found no technology path eligible for retirement. Task 14 recorded `NO_CUTOVER` for both MOD-001 and MOD-012, so no replacement runtime/SDK has taken canonical ownership. The M6 design also requires post-switch merged-main observation evidence before removing an old path; this stacked modernization branch has no such merged-main cutover observation to cite. Deleting Python 3.11, .NET 8, or either compatibility lane here would therefore violate the frozen retirement gate rather than complete it.

| Surface | M6 disposition | Reason |
| --- | --- | --- |
| Python 3.11 canonical lane and `requires-python >=3.11` / Ruff `py311` | `RETAIN` | At Task 15, MOD-001 was `APPROVED / APPROVED`; M5 did not switch canonical ownership. |
| Python 3.14 compatibility lane | `RETAIN` | It remains candidate compatibility evidence, not an obsolete old baseline. |
| Root .NET 8 SDK policy and Revit Core `net8.0` canonical target | `RETAIN` | At Task 15, MOD-012 was `APPROVED / APPROVED`; M5 did not switch repository-wide SDK ownership. |
| Host-neutral .NET 10 compatibility lane | `RETAIN` | It remains an opt-in compatibility proof and is not an obsolete legacy path. |
| AutoCAD/Revit native Host targets | `RETAIN` | T3 Host version ↔ runtime ↔ TFM ownership remains governed by the Task 13 Host matrix. |
| V1/V2 and architecture compatibility bridges (MOD-016) | `RETAIN / DEFER_ARCHITECTURE` | They are T4 Architecture Modernization Review inputs and are explicitly outside M6 cleanup authority. |

No `.github/workflows/repository-regression.yml`, runtime metadata, SDK selector, native project, or compatibility-lane implementation is removed by Task 15. Retirement may be reconsidered only after a separately eligible M5 cutover is merged to `main`, its required merged-main observation is green, and any applicable Host evidence is present.
